<%= form_for(@support_number) do |f| %>
  <% if @support_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@support_number.errors.count, "error") %> prohibited this support_number from being saved:</h2>

      <ul>
      <% @support_number.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group row">
  <div class="field">
    <%= f.label :name, class: "col-sm-2 col-form-label col-form-label-lg"  %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "col-sm- form-control form-control-lg"  %>
    </div>
  </div>
  </div>
  <div class="form-group row">
  <div class="field">
    <%= f.label :number, class: "col-sm-2 col-form-label col-form-label-lg" %>
    <div class="col-sm-10">
      <%= f.text_field :number, class: "col-sm- form-control form-control-lg" %>
    </div>
  </div>
  </div>
  <div class="actions">
    <%= f.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
